<div class="row body border-bottom" id="services-row">

    <h2 class="text-center">SERVICES</h2>

    {% set colNbr = 12 / services|length %}

    {% for service in services %}
        <div class="col-sm-{{ colNbr }} text-center service-container">
            <img src="{{ asset('uploads/images/services/') ~ service.image }}"/>
            <div class="service-title">{{ service.title }}</div>
            {% if service.description is defined %}
                <p>{{ service.description }}</p>
            {% endif %}
        </div>
    {% endfor %}
</div>